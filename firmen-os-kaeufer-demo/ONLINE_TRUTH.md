# Online-Wahrheitsstatus · Firmen OS Käufer Operations Demo

**Stand:** 11. Juli 2026  
**Status:** ONLINE-QUELLE BELEGT · LIVE-HTTP NOCH OFFEN

## Belegt

- Das Repository `Pagebabe/portfolio` ist öffentlich.
- Der Standardbranch ist `master`.
- Der finale Abgabe-Commit `cc25a92483589f8e304f911ea05637cc22de34ee` liegt auf `master`.
- Der öffentliche Ordner `firmen-os-kaeufer-demo/` enthält HTML, JavaScript, CSS, Manifest, Service Worker, Icon, Wahrheits-Audit, Abgabeanleitung und Live-Abnahme.
- Der Submission-CI prüfte JavaScript-Syntax, Rollenbegrenzung, fünf Betriebstage, mindestens 30 Vorgänge, mindestens 12 Übergaben, Datenschutzmarker, Manifest, Service-Worker-Scope, Wahrheitsstatus und lokale HTTP-Auslieferung erfolgreich.
- Die Demo enthält ausschließlich synthetische Geschäftsdaten und keine echte `@kaeufer.de`-Adresse.
- Die öffentliche Demo schreibt keine Daten nach Supabase.

## Online-Ziel

`https://pagebabe.github.io/portfolio/firmen-os-kaeufer-demo/`

## Noch nicht unabhängig belegt

Der direkte HTTP-Aufruf aus der aktuellen Prüfumgebung konnte wegen fehlender DNS-Auflösung für `pagebabe.github.io` nicht ausgeführt werden. Das ist weder ein PASS noch ein FAIL der Website.

Bis ein Browser die URL tatsächlich lädt, bleiben offen:

1. HTTP-Erreichbarkeit der GitHub-Pages-URL
2. vollständig gerenderte Live-Oberfläche
3. Desktop-, Tablet- und Smartphone-Sichtprüfung
4. echte PWA-Installation über HTTPS
5. praktischer Offline-Neulade-Test

## Verbindliche Aussagegrenze

Zulässig:

> Die Abgabedateien und der Wahrheitsstatus sind auf dem öffentlichen `master`-Branch veröffentlicht und durch CI geprüft. Der direkte Live-HTTP-Aufruf bleibt bis zum Browserbeweis offen.

Nicht zulässig:

- „Live vollständig geprüft“
- „auf allen Geräten getestet“
- „PWA-Installation bewiesen“
- „Offline-Funktion produktiv bewiesen“
- „100 % betriebsfertig“

## Benötigter letzter Netzbeweis

Ein Screenshot der vollständig geladenen Live-URL genügt für den Erreichbarkeitsnachweis. Ein zweiter Screenshot in schmaler Smartphone-Ansicht schließt den sichtbaren Responsive-Gate.
